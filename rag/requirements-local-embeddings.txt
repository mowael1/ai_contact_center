# Optional: run google/embeddinggemma-300m locally instead of via the
# HuggingFace Inference API. Pulls in torch (~2 GB) and ~1.2 GB of weights.
# Set EMBEDDING_PROVIDER=embeddinggemma_local afterwards.
sentence-transformers>=5.0
