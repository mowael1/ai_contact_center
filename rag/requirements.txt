# RAG backend runtime dependencies.
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.9
pydantic-settings>=2.5
typer>=0.12
rich>=13.7
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
pyarrow>=17.0
pandas>=2.2
chromadb>=1.5
python-dotenv>=1.0

# Agentic retrieval graph. Only langgraph itself is used; langchain-core
# arrives as its transitive dependency and none of its abstractions are used.
langgraph>=1.0

# Tests
pytest>=8.3
